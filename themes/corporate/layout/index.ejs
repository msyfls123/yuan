<%
  var primary = site.categories.data
		.find(cat => cat.name === config.main_categories[0])
  var secondary = site.categories.data
		.find(cat => cat.name === config.main_categories[1])
	var primaryPosts = primary.posts
		.filter(page => !!page.thumbnail)
		.sort('date', 'desc').sort('weight', 'desc')
		.slice(0, 3)
	var secondaryPosts = secondary.posts
		.filter(page => !!page.thumbnail)
		.sort('date', 'desc').sort('weight', 'desc')
		.slice(0, 3)
%>
<div class="container-fluid flush-with-header">
	<div class="margin-bottom-40 row-corporate">
		<div class="bg-index-banner"></div>
		<div class="container">
			<div class="col-xs-12 row-corporate-text-container">
				<div class="row-corporate-text">
					<h4>数字化体验创新生长力</h4>
					<h1>数字设计 体验咨询<br>
					创新与成长 & 合作探究</h1>
					<p>
						除了企业项目，也喜欢喜欢探究人与物，文化、商业模式和经济形势，<br>
						引入多元视角思考，运用独有的逻辑和方法去解析诠释。
					</p>
					<a class="btn btn-success" href="<%- url_for('/contact') %>">与我联系</a>
				</div>
			</div>
			<!-- <div class="col-xs-4 col-sm-6">
				<img src="/assets/bg-banner.png">
			</div> -->
		</div>
	</div>
	<div class="container index-main">
		<div class="row showcase-section">
			<div class="col-xs-12 col-md-2">
				<h2><%- primary.name %></h2>
			</div>
			<div class="col-xs-12 col-md-10">
				<ul class="banner-list">
					<% primaryPosts.forEach((post) => { %>
						<li>
							<a
								href="<%- url_for(post.path) %>"
								class="primary-banner"
								style="background-image: url('<%- url_for(post.thumbnail) %>');"
							>
								<header>
									<p><%- post.subtitle %></p>
									<h3><%- post.title %></h3>
								</header>
							</a>
						</li>
					<% }) %>
				</ul>
				<a href="<%- url_for('/archives/') %>" class="btn btn-success">
					更多 <i class="fa fa-arrow-right"></i>
				</a>
			</div>
		</div>
		<div class="row showcase-section">
			<div class="col-xs-12 col-md-2">
				<h2><%- primary.name %></h2>
			</div>
			<div class="col-xs-12 col-md-10">
				<p class="intro">
				  除了企业项目，也喜欢喜欢探究人与物，<br>
				  文化、商业模式和经济形势，<br>
				  引入多元视角思考，<br>
					运用独有的逻辑和方法去解析诠释。
				</p>
				<ul class="banner-list">
					<% secondaryPosts.slice(0, 1).forEach((post) => { %>
						<li>
							<a
								href="<%- url_for(post.path) %>"
								class="primary-banner"
								style="background-image: url('<%- url_for(post.thumbnail) %>');"
							>
								<header>
									<p><%- post.subtitle %></p>
									<h3><%- post.title %></h3>
								</header>
							</a>
						</li>
					<% }) %>
				</ul>
				<%- partial('_partial/card-list', { posts: secondaryPosts.slice(1, 3) }) %>
				<a href="<%- url_for(secondary.path) %>" class="btn btn-success">
					更多 <i class="fa fa-arrow-right"></i>
				</a>
		  </div>
		</div>
	</div>
</div>

